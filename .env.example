# NeonPanel env
ADMIN_PASS=                # empty = random at first run (printed once in log)
SECRET_KEY=               # empty = auto
DATA_DIR=/data
PORT=8080
TCP_PORT=0                # 0 = off; set to expose raw VLESS/TCP on this port
LOG_LEVEL=info